# Indexes

Generated indexes live here.

Run:

```bash
python tools/lughbrugh.py index
```

Indexes are committed support artifacts, not the sole source of truth. They must be rebuildable from human-readable files.

