# Open Points

- Should future template releases include a dedicated `canon/stubs/` folder, or keep stubs wherever the project chooses?
- Should seed extraction have a machine-readable export later?
- Should local background models receive a default prompt pack for stub extraction and drift reports?
