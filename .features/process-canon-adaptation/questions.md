# Open Points

- Should a future tool generate thing stubs automatically into a selected folder?
- Should process guidance updates have their own review command?
- Should background local models receive a dedicated prompt pack for process drift and thing extraction?
