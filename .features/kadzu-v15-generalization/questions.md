# Open Points

- Should `tools/lughbrugh.py` eventually support an export command?
- Should facet routing produce machine-readable route recommendations?
- Should system profiles become formal fixtures for validation?
